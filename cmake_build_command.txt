# creating clean state
rm -rf build

# building the executable
cmake -S . -B build
cmake --build build

# running the app on linux after building
./build/CellularAutomata

# running the app on Windows after building
./build/Debug/CellularAutomata.exe

